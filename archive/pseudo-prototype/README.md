# Current Pre-Prototype insecure and inefficient workflow

The money is never actually borrowed, rather the user's action is performed by the contract, this wy we never need to use an "advanced signature".

